# PM Atlas 项目实验室 Handoff

- 更新日期：2026-08-29
- 当前案例：`car-control:v5`（新分支默认版本；内容哈希 `a1b0a1b786181e520f17755c4975c68c23e24fb3311d6b0a3681ece806b205c1`）
- 最新发布：Sites `v57`（知识库原地侧栏与“删除我的实验室数据”）；源码为本 handoff 所在提交
- 本地待发布：无
- 最近验证：103 项自动化测试、相关文件 ESLint、公开投影隔离检查和生产构建全部通过
- 线上地址：`https://pm-atlas-iman.ardent-pixie-5731.chatgpt.site/#lab-schedule`
- 产品依据：`specs/项目实验室MVP-PRD-v0.1.md`
- 技术依据：`specs/项目实验室MVP-技术方案-v0.1.md`

## 1. 当前阶段

项目实验室已经完成“观察主线 → 从节点接手 → 阅读事件材料 → 创建三池行动链 → 提交并推进一周 → 查看客观结果和管理缺口”的纵向闭环，也已具备业务字段 Patch、基础文件差异、主线/分支指标比较、结局复盘、AI 结构化复盘接口以及接手记录切换。

文件差异已升级为主线/分支逐字段并排视图，并按分支绑定的 `caseVersion + contentHash` 重建同周主线语义快照；路径比较也已升级为 Git 式多回合图，串联分叉点、每回合状态哈希、同期主线指标、分支指标和文件修订。情景结算后会进入完整结局页，先展示不可被 AI 覆盖的确定性规则结论、指标差异和文件证据，再按需展示 AI 的优势、改进项、主线差异、能力画像、知识条目和重试建议。32 份项目文件和 P0 复盘闭环均已完成；不再调整已经稳定的行动链交互。

## 2. 当前行动链产品规则

1. 编辑区固定为“项目文件 → 工具与技术 → 干系人”三个卡池。
2. 用户点击候选卡后，卡片自动进入对应池子；卡片均使用知识库或案例登记册中的正式编号和标题。
3. 每条行动链必须填写行动目标，并在三个池子中各选择至少一张卡。
4. 点击“确定”后，当前组合固化为一条行动链，行动目标作为链名称，编辑区自动清空。
5. 已固定行动链支持编辑、复制、删除和排序；至少固定一条链后可提交并推进一周。
6. 编辑阶段不提示答案，也不显示决策依据或自动引用区。
7. 提交后的管理缺口按必要动作显示未闭环影响、已识别卡片、具体尚缺卡片、跨链分散和未完成前置动作。

## 3. 当前结算规则

1. Worker 只接收结构化 `actionChains`，不接收用户手工连线。
2. 对每个必要管理动作，服务端读取其可见支持卡；这些卡全部位于同一条行动链且前置动作已完成时，该动作完成。
3. 已完成动作保存在分支状态中，可跨回合累计；后续回合不需要重复选择。
4. 情景闭环只取决于必要管理动作是否全部完成，以及安全、成本、进度等终局规则；不存在独立的“全局连接完整度”门槛。
5. 行动目标文本用于表达管理意图和后续复盘，不参与关键词判分。
6. 有帮助但非必要的可见卡会产生管理负载；可见有害卡按规则应用一次性后果。
7. 未闭环时应用持续恶化规则并推进一周；完整闭环后按近主线、绕路、延期或失败路径结算。
8. AI 不参与项目状态和路径判定，只能在后续复盘层解释规则结果。

冻结案例包 v1–v4 中仍可能保留历史 `minimumCorrectConnections` 和私有执行卡字段，以维持原内容哈希及既有分支可读性；当前引擎、客户端和闭环判定均不读取这些字段。D1 的 `connections_json` 也是历史物理列名，当前实际保存结构化 `actionChains`。

## 4. 已完成能力

- 32 周主线、6 个里程碑和 W9/W17/W25 三个接手点。
- 15 项总览仪表、10 个管理领域入口、11 个一级 WBS 和 35 项活动。
- 15 名干系人、动态 RACI、CCB、风险、需求、工作量、甘特图、燃尽图和时标网络图。
- 32 份项目文件目录；D01–D32 已全部具备专用业务内容。
- 平台登录、个人分支创建、分支归属、案例版本与内容哈希校验、URL 恢复。
- v1–v4 冻结案例包随部署保留；历史分支按 `caseVersion + contentHash` 精确恢复对应主线、项目文件和情景规则。
- 接手记录合并展示 v1–v5；只有当前 v5 可以创建新分支。
- 接手记录抽屉支持返回主线、查看和切换个人分支；情景结束后可清除当前分支界面上下文并前往下一接手点。
- 情景材料逐项打开、查看事件记录、材料读完后解锁候选卡。
- 三池行动链编辑、复制、排序、删除、云端草稿自动保存和直接提交。
- 服务端回合校验、乐观并发、幂等重放、确定性周结算和不可变快照。
- SPI、CPI、AC、预测完工、追踪覆盖率、风险、干系人和文件修订联动。
- 分支结算会为 D03、D05、D09、D10、D13、D14、D16、D17、D20、D21、D22、D24、D26、D27、D28、D29、D30 等受影响文件写入可审计的业务字段 JSON Patch，而不是只写修订标记。
- 文件抽屉按分支绑定的冻结案例版本和内容哈希重建同周主线语义快照，并将当前有效业务字段以主线值/分支值并排展示，明确区分新增、修改和删除；页面也已提供主线/分支关键指标比较、情景结局说明和 DeepSeek 结构化复盘接口。
- Git 式多回合路径图按 round 0 分叉快照和后续不可变状态快照回放，逐回合显示短状态哈希、同期主线与分支 SPI/CPI/完工预测、结算状态和文件修订；文件节点可直接打开对应项目文件。
- 情景结算后自动打开完整结局页；确定性层展示路径分类、终局状态、回合与动作、文件修订、追踪覆盖、增量成本和主线/分支指标差异，并可从证据节点打开对应项目文件。
- DeepSeek 复盘作为可选增强，完整展示 `summary`、优势、改进项、主线差异、五维能力画像、知识条目和重试建议；服务端按分支冻结案例包构建上下文，对七个字段、证据引用和能力等级做完整规范化校验，不合格结果不会缓存或渲染。
- 接手记录按 W9/W17/W25 情景归集多次尝试，尝试序号按实际创建顺序派生；分支支持最多 40 字的自定义名称。
- 结局页支持“从此节点重试”：始终基于当前 `car-control:v5` 接手点创建全新分支，并通过 `parent_branch_id` 保留重试来源；旧分支的冻结版本、快照、结局、文件差异和 AI 复盘不被修改。
- 行动卡中的 D01–D32 / T001–T133 正式编号和 AI 复盘的有效知识编号可在情景内打开知识侧栏；侧栏复用正式知识库内容，不跳转页面，也不重置分支、周次、材料或行动链草稿。干系人角色和 AI 自造编号不会被伪装成知识条目。
- 账号菜单提供“删除我的实验室数据”：用户必须输入“删除”二次确认，服务端再按平台身份校验归属，并在一个 D1 批处理中删除当前用户的分支、回合、草稿、材料阅读、文件差异、AI 复盘、进度与分析记录；ChatGPT 账号、其他用户数据和共享冻结案例包不受影响。删除成功后当前情景上下文和接手记录会立即复位到主线。
- 提交后具体管理缺口诊断；不会再生成旧版“尚未连接为完整闭环”缺口。
- 服务端私有案例包与前端公开投影隔离，客户端不包含判分字段。
- 文件抽屉已移除 D02、D10、D16、D31 等页面中重复的大黑色摘要区；主要宽表已收缩到画布宽度，文件列表可滚动到 D32。

## 5. 当前优先级

### P0：完成复盘闭环

32 份文件已全部补齐，复盘界面可以直接引用正式的 D20 质量结论和 D27 风险结论。

- [x] 分支文件已写入真实业务字段 JSON Patch；支持 `add`、`replace` 和 `remove`，并随回合不可变保存。
- [x] 文件抽屉已实现逐字段并排 diff：服务端按分支的 `caseVersion + contentHash + 当前周` 重建对应主线语义快照，顺序应用多回合 Patch 后只返回当前有效差异，并明确区分新增、修改和删除；W32 后以 W32 归档主线为比较基准。
- [x] 将当前 SPI、CPI、预测完工摘要升级为 Git 式多回合路径图，串联分叉点、每回合提交、同期主线快照、文件修订和最终结局；W32 后继续与 W32 归档主线比较。
- [x] 将当前内嵌结局区升级为完整情景结局页：展示确定性规则复盘，并完整渲染 AI 返回的优势、改进项、主线差异、能力画像、知识条目和重试建议。

### P1：分支和知识联动

- [x] 实现接手记录列表、v1–v5 合并展示和分支切换。
- [x] 增加分支命名、同一情景多次尝试分组，以及明确的“从此节点重试”操作。
- [x] 接入知识库原地侧栏，支持从行动卡和 AI 复盘知识编号查看正式知识条目，并保持情景上下文。
- [x] 实现“删除我的实验室数据”：账户菜单二次确认、服务端身份归属校验、事务化删除和前端上下文复位均已完成。

### P1：文件与仪表真实性

32 份文件的专用业务内容已全部完成；后续不再为填满页面添加重复文案，只在权威数据或业务事件确有变化时更新。

#### 第一批：补齐 W9 情景依据

- [x] D04 估算依据：只记录成本与工期的估算方法、关键假设和数据来源。
- [x] D06 成本估算：记录人工、非人工、风险准备金、BAC 以及批准变更后的最新估算。
- [x] D13 项目沟通记录：只记录关键决策、审批、承诺和升级，不写普通会议纪要。

第一批已完成并纳入 `car-control:v5`。三个文件均从案例权威数据派生，不新增重复事实；D06 已补齐 W9 成本依据，D13 会随时间轴汇总三个情景涉及的关键记录。

#### 第二批：补齐资源文件并支撑 W17

- [x] D12 项目日历：工作周、迭代节奏和阶段门；不存在于权威数据源的休息日不编造。
- [x] D23 资源分解结构：只展示八类核心人员和四类非人力资源的分类与控制边界。
- [x] D25 资源需求：按 35 项活动展示角色人日，另列四类非人力资源的需求窗口。
- [x] D24 资源日历：按周展示角色计划投入、可用容量及不可用/批准加班例外。
- [x] D17 项目团队派工单：展示实际角色派工、主要 WBS、生效周期和交接状态。
- [x] D11 物质资源分配单：按所选周展示四类非人力资源的计划、累计分配和剩余金额。

第二批已完成并纳入 `car-control:v5`。六份视图均从案例权威数据派生；D17、D24 会随时间轴变化，并在 W17 情景分支记录车端工程师不可用、临时覆盖、结构化交接和 W18 后端批准加班等字段级差异。D11 不把风险储备作为可分配资源，也不虚构设备台数、车辆数量或法定节假日。

#### 第三批：补齐进度控制文件

- [x] D28 进度数据：显示数据日期、PV/EV/AC、SPI/CPI，以及本周状态变化和当前零浮动活动的实际开始、完成状态与证据。
- [x] D29 进度预测：显示基线、上次预测、当前完工预测、偏差、零浮动活动、关联风险和里程碑偏差。
- [x] D01 活动属性：只补充 D02 未展示的关键性、浮动、并行启动和提前量/滞后量控制属性。
- [x] D07 持续时间估算：显示 33 项离散活动的 O/M/P、PERT 期望工期、标准差和方差，不重复 D04 的方法说明。
- [x] D15 项目进度网络图：显示零浮动活动序列以及全部离散活动的逻辑关系、早晚日期、总浮动和自由浮动。

第三批已完成。五份视图全部从 D02/D14、`schedulePlan`、`baselineWorkload`、里程碑和风险状态派生，没有新增案例事实或改变 `car-control:v5` 内容哈希；D28/D29 支持个人分支当前回合指标和业务字段 Patch。表格在桌面端使用画布宽度，不依赖横向滚动。

#### 第四批：补齐质量闭环并支撑 W25

- [x] D19 质量测量指标：指标定义、阈值、测量方法、负责人和质量门。
- [x] D18 质量控制测量结果：各轮实际测量值、达标结论和缺陷证据。
- [x] D20 质量报告：质量趋势、未通过项、残余风险和发布建议。

D19、D18、D20 已形成完整质量闭环：D19 维护 3 项硬性质量门和 7 项绩效指标的批准口径，D18 展示截至所选周的最近实测值、阈值、结论与 D32 证据引用，D20 只汇总趋势、当前例外、残余风险和发布建议。W25 分支会把远程控制质量门失败、只读能力通过、范围处置和发布建议写成字段级差异；三份视图均从现有权威数据派生，没有修改 `car-control:v5` 内容哈希。

#### 最后补充

- [x] D27 风险报告：只汇总阶段性风险趋势、最高暴露项和整体风险结论，不复制 D26 风险登记册逐条内容。

D27 按 W4/W8/W12/W20/W28/W32 及当前所选周汇总风险数量与等级变化，只展示最多 3 项最高暴露风险，并引用 D26、D08 和 D29 形成整体结论。个人分支兼容 `lifecycleState` 与 `toLifecycleState` 两种冻结案例字段，并把风险状态、控制状态、完工预测和管理结论写入字段级 Patch。

- [ ] 将燃尽图从固定模板改为真实迭代任务数据。
- [ ] 补齐当前项目健康状态、异常优先级和管理领域详情。

### P2：工程质量

- [ ] 拆分体量较大的 `lab-timeline-page.tsx`。
- [ ] 增加材料阅读、三池行动链、多回合闭环、文件差异和复盘的浏览器端到端测试。
- [ ] 完善 API 延迟、结算失败、AI 调用和案例错误观测。

## 6. 项目文件内容约束

1. 一个业务事实只维护在一个权威数据源中，其他文件通过正式编号引用或派生展示。
2. 文件只在真实事件、审批或数据变化时更新，不为“本周无变化”制造新版本。
3. D13 不写流水账式会议纪要；D27 不复制风险登记册；D20 不复制测试用例和测量明细；D29 不重抄整个进度计划。
4. D17 只记录实际派工与交接，团队职责规则保留在 D31，RACI 继续使用现有矩阵。
5. D24 只记录可用性和容量例外，不重复 D17 的工作分配；D11 只记录非人力资源的实际分配，不重复 D25 的资源需求。
6. D04 只解释估算方法和假设，D06/D07 保存估算结果；D01/D07/D15 复用 D02/D14 的活动和进度源数据。
7. 优先从现有 workload、schedule、stakeholder、requirement、risk、quality 和 document plans 派生专用视图；如果必须增加会改变案例内容哈希的新业务数据，应创建 `car-control:v6`，不得直接修改已被线上分支引用的 v1–v5。

## 7. 开发约束

1. `car-control:v1` 至 `v5` 都可能被线上分支引用。v1–v4 由 `lab-case-history.generated.ts` 冻结保存，v5 是当前源版本；任何会改变 v5 内容哈希的后续案例调整必须创建 v6。
2. 可见工具卡必须使用知识库 `tool:xxx` 与 `Txxx` 编号和正式标题；项目文件使用 D01–D32；干系人使用案例登记册 ID。
3. 不把判分字段、必要动作映射、有害后果或未来材料投影到前端包。
4. 规则引擎必须保持确定性和幂等；AI 不得覆盖结算状态。
5. 当前三池行动链就是正式交互，不重新引入第四栏、手动画线、决策依据或自动引用编辑器。

## 8. 恢复开发建议

P0 复盘闭环、P1 分支重试、知识库原地侧栏和个人实验室数据删除均已完成。下一步优先把燃尽图从固定模板改为真实迭代任务数据：先建立最小的迭代任务权威数据源，再由燃尽图派生展示，避免把计划活动、任务剩余量和固定演示曲线混在一起。完成后再补项目健康状态、异常优先级和管理领域详情；不要先增加新的装饰性面板。

`drizzle/0002_gorgeous_johnny_blaze.sql` 已从 Sites v56 起应用，v57 发布包继续保留该迁移。旧分支的 `branch_name` 保持可空默认值，不做数据回填。“删除我的实验室数据”复用现有外键和 D1 批处理，不需要新增迁移。
