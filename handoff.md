# PM Atlas 项目实验室 MVP Handoff

- 日期：2026-08-04
- 当前线上版本：Release 27
- 线上地址：`https://pm-atlas-iman.ardent-pixie-5731.chatgpt.site/?release=27#lab-schedule`
- 审查依据：`specs/项目实验室MVP-PRD-v0.1.md`、`specs/项目实验室MVP-技术方案-v0.1.md`、当前代码与线上页面
- 本文目的：记录当前实现状态、与 MVP 目标的差距、已发现问题和建议推进顺序

## 1. 总体判断

当前实现不是完整 MVP，更准确的定位是：

> 阶段 1“只读主线工作台”已具备较完整的可视化和六份真实动态文件；阶段 2已完成“创建分支、发现材料、编辑行动链并保存草稿”，下一步进入回合提交与状态结算。

页面已经形成明确的项目实验室外观和基础交互，但“观察 → 判断 → 行动 → 推进 → 反馈 → 对比 → 复盘”的核心学习闭环尚未打通。

## 2. 当前已完成

- 黑白灰视觉风格基本统一。
- 已展示 32 周主线、6 个里程碑和 W9/W17/W25 三个接手点。
- 已展示 15 项总览仪表和 10 个管理领域入口。
- 时间轴变化会联动 SPI、CPI、AC、风险、干系人和文件状态。
- 时间轴支持完整模块划过后吸顶、键盘左右键逐周推进和里程碑门标记。
- 已展示完整 11 个一级 WBS、35 项活动时标网络图和可展开的二级 WBS 卡片。
- 项目工作量展示当前周前后各 10 周共 21 周，迭代燃尽图区分理想参考线与实际阶梯线。
- 已展示 15 名干系人、动态 RACI 和 CCB 成员悬浮信息。
- 已展示 32 份项目文件目录、状态、历史事件和文件关联。
- D22 需求跟踪矩阵和 D26 风险登记册已展示按周变化的结构化内容。
- D05 变更日志、D08 问题日志、D21 需求文件和 D32 测试与评估文件已展示按周变化的结构化内容。
- CCB 待办由8项真实变更记录驱动，RACI按当周主要工作包动态展示中文角色。
- 已实现平台登录身份、个人分支创建、分支归属校验和 URL 恢复。
- 已实现情景材料逐项打开、查看事件记录和候选行动卡解锁。
- 已实现四栏行动卡选择、相邻列连接、排序、删除和行动链完整性检查。
- 已实现三段式决策依据、20–500 字校验以及事件材料、项目文件、行动卡引用。
- 已实现登录用户行动链草稿的云端读取、700ms 防抖自动保存、分支归属和引用合法性校验。
- 已完成服务端私有案例包与前端公开投影隔离。
- 当前案例校验通过：32 份文件、15 名干系人、35 个活动、10 项风险、3 个情景。
- 当前 19 项自动化测试全部通过。

## 2.3 本次工作记录（Release 26）

- 新增个人分支当前回合草稿 `GET/PUT /api/lab/branches/:branchId/scenarios/:scenarioId/draft`。
- 草稿按“分支 + 下一回合”唯一保存，刷新或通过 URL 恢复分支后会自动回填。
- 服务端校验卡片属于当前情景、连线只跨相邻列、材料已经打开、文件在当前周已经创建、行动卡已经选择。
- 候选行动卡改为可选择的四栏工作台；已选卡支持排序、删除和建立/删除连接。
- 决策依据包含“观察到的信号、风险或根因判断、行动理由”三项，每项 20–500 字。
- 引用区支持已打开事件材料、当前可用项目文件和已选行动卡，并显示草稿完整性状态。
- 本阶段只保存草稿，提交按钮保持禁用；回合提交、状态引擎和客观反馈继续作为下一阶段工作。

## 2.4 本次工作记录（Release 27）

- 新增 `POST /api/lab/branches/:branchId/rounds`，校验登录身份、分支归属、预期回合、幂等键、行动链、决策依据和引用。
- 新增确定性回合结算器，按“必要动作→有害效果→持续恶化→推进一周→终局规则→派生状态”固定顺序运行。
- 必要动作和正确连接可跨回合累计；孤立卡片不计入完成度；有害卡成本与工作量每分支只结算一次。
- 回合提交通过 D1 原子批处理写入提交记录、不可变状态快照、项目事件、文件增量并更新分支周次和乐观锁。
- 同一幂等键重试返回原结算结果，不重复推进周次、增加成本或生成文件版本。
- 前端启用“提交行动链并推进一周”，即时展示 SPI、CPI、当回合成本、预测完工周、需求追踪覆盖率和管理缺口类别。
- 分支状态会联动总览 SPI、CPI、AC、风险、干系人和项目文件分支版本；刷新或 URL 恢复后继续显示上一回合结果。
- 情景一完整最小行动集合可在 W10 进入近主线成功；不完整行动会保持开放并产生每周协调成本与追踪覆盖率下降。
- 新增状态引擎单元测试和真实 SQLite 回合提交、幂等、快照及事件持久化测试。

## 2.1 本次工作记录（2026-08-04）

### 主线工作台与图表

- 甘特图调整为完整展示 11 个一级 WBS，并压缩横向尺度避免 W32 被裁断。
- 时标网络图展示完整 35 项活动、依赖关系、关键路径和浮动时间；去除背景网格，增加横纵坐标悬浮提示。
- WBS 改为 11 张无描边纯色卡片，默认只显示一级标题，展开后纵向显示二级任务。
- 时间轴增加轻量吸顶模式、里程碑圆环、当前周标记和键盘左右键控制。
- 项目工作量调整为 21 周窗口；燃尽图改为带周末平台的理想线和实际阶梯线。
- CCB 待办标题增加成员图标与名单悬浮层；干系人参与度补齐为 15 人。

### 风险与需求数据

- 新增 `requirement-plan.json`，包含 24 项 V1.0 核心需求和 6 项 P2/P3 后续版本候选需求。
- 需求与 WBS 按多对多关系建模，使用主要 WBS、支持 WBS 和候选建议 WBS 区分基线责任。
- 需求仪表不再硬编码 24 项，改为按发现、基线、开发完成、验证和候选状态逐周统计。
- 10 项风险补齐原因、事件、后果、影响范围、发现周、应对措施、责任人、应对完成周、关闭周和处理结果。
- 风险按发现周进入矩阵，应对完成前使用固有评估，应对完成后使用剩余评估，关闭后退出活动矩阵但保留历史统计。
- R04 主线于 W24 关闭，情景三允许在 W25 重新触发，并保留关闭与重开历史。

### 项目文件与发布

- D22 在 W3 后展示需求跟踪矩阵，W28 增加 6 项“纳入后续版本”的试点候选需求。
- D26 从 W2 起按风险发现周逐步增加登记项，并显示等级、影响、措施、负责人、关闭周和结果。
- 项目文件抽屉总宽保持不变，左侧导航由约 390px 缩至 210px，右侧正文获得更多空间。
- Release 19 发布风险与需求动态文件；Release 20 发布文档抽屉排版优化。
- 关键提交：`7668822`（动态风险与需求矩阵）、`8092b87`（文档详情区域扩宽）。

## 2.2 本次工作记录（Release 23）

- 建立8项正式变更请求及CCB成员、法定人数、审查、决议、执行和关闭规则。
- CCB仪表不再按阶段门距离推算，改为展示当前待评审、审查中、执行中、待关闭和已关闭变更。
- D05变更日志接入变更事项、影响评估、决议、负责人和关闭结果。
- RACI继续按当周投入最高的活跃工作包动态选择，并将全部内部角色ID转换为中文名称。
- 风险与需求仪表详情增加状态统计、筛选、明细表和关联文件入口。
- D08问题日志、D21需求文件和D32测试与评估文件补齐结构化正文及按周版本事件。
- 项目文件抽屉中的需求和风险表格固定列宽，完整显示末列且无需横向滚动。

## 3. P0：MVP 核心闭环阻塞项

- [x] 行动链支持卡片选择、相邻列连接、排序和删除。
- [x] 已实现三段式决策依据及 20–500 字必填校验。
- [x] 已实现决策依据引用标签和登录用户云端草稿自动保存。
- [x] 已实现回合提交 API、乐观并发和幂等重放。
- [x] 已实现确定性项目状态引擎的首个纵向切片。
- [x] 用户行动可改变 SPI、CPI、AC、预测完工、风险、干系人和文件版本；范围与质量的完整派生视图仍需继续深化。
- [x] 分支按每回合一周推进并保存不可变快照。
- [x] 已实现当回合客观后果和管理缺口类别反馈。
- [x] 行动可生成分支文件增量和新版本标记。
- [ ] 尚未实现文件新增、删除、修改的 Git 式字段差异。
- [ ] 尚未实现主线与个人分支的 Git 式轨道比较。
- [ ] 尚未实现情景结局、能力画像和 AI 复盘。
- [ ] 尚未实现分支列表、命名、切换、多次尝试和历史节点重试。
- [ ] 尚未接入知识库原地侧栏。
- [ ] 尚未实现“删除我的实验室数据”。

## 4. P1：仪表盘真实性问题

- [x] 已新增 30 项需求计划，基线、完成、验证和候选状态均来自需求数据。
- [x] 24 项核心需求统一在 W8 进入基线，W28 新增 6 项后续版本候选需求。
- [ ] 当前迭代燃尽图使用固定 34 点模板，不是真实迭代任务和剩余工作数据。
- [x] CCB 待办由真实变更项的提交、评审、决议、执行和关闭周驱动。
- [x] 甘特图完整展示 11 个一级 WBS，并覆盖 W1–W32。
- [x] 时标网络图完整展示 35 项活动、时间刻度、依赖关系、关键路径和浮动时间。
- [x] WBS 使用 11 张一级卡片，并可展开查看二级任务。
- [x] 风险按发现周进入活动视图，并区分已评估、监控中和已关闭状态。
- [x] RACI按当周主要工作包动态显示，并将内部ID转换为中文角色名称。
- [x] 风险和需求仪表已提供筛选、明细、来源和关联文件入口；其余仪表仍为简要事实。
- [ ] 尚未形成明确的“当前项目健康状态”和异常优先级。

相关临时派生逻辑主要位于 `prototype/app/lab-timeline-page.tsx`。

## 5. P1：项目文件问题

- [x] D22 需求跟踪矩阵和 D26 风险登记册具备按周变化的结构化真实内容。
- [ ] 其余 26 份项目文件仍以摘要、关联和版本事件为主，尚无完整正文。
- [ ] `document-plan.json` 当前主要包含 32 条文件元数据、5 个聚合主线事件和文件关系，没有结构化文件正文。
- [ ] 当前版本号由文件命中的主线事件数量计算，不是真实修订号。
- [ ] 缺少负责人、更新时间、审批人、审批状态和变更来源。
- [ ] 缺少按阶段、状态和“本周更新”筛选。
- [ ] 只能高亮关联文件，不能高亮关联仪表、管理领域和情景。
- [ ] 从仪表异常或情景材料不能定位到文件的具体版本和字段。
- [ ] 已完成D05、D08、D21、D22、D26、D32六份动态文件；其余核心动态文件尚待补齐。
- [ ] 尚未实现其余 14 份支撑文件的真实内容和关键版本。

## 6. P1：管理领域问题

- [ ] 点击 10 个管理领域当前只是打开并筛选文件抽屉，不是独立领域详情视图。
- [ ] 缺少领域当前状态、历史趋势和详细图表。
- [ ] 缺少当前阶段相关管理过程、工具与技术。
- [ ] 缺少相关干系人、待办、风险、变更和决策记录。
- [ ] 缺少与其他管理领域的影响关系。
- [ ] 管理领域与文件映射仍硬编码在页面组件中，没有完全由案例配置驱动。

## 7. P1：主线与分支体验问题

- [ ] 第一屏主要由项目标题和时间轴占据，核心仪表盘大部分位于首屏以下，不利于边拖动边观察。
- [ ] 当前时间轴从 W1 开始，但 PRD 验收标准写的是 W0–W32。
- [ ] W1 已经显示完整未来 WBS 和计划，容易让用户误以为项目授权时所有基线已经确定。
- [ ] 接手节点没有执行已确认的四步解锁条件：到达节点、展开主线处理、打开关键证据文件、主动确认了解主线。
- [ ] 创建分支后仍可拖动上方主线，但下方分支固定在接手周，主线与分支上下文容易混淆。
- [ ] 没有显示分支名称、保存状态、当前回合和云端同步状态。
- [ ] 邮件、报告和消息目前主要以事实列表呈现，材料拟真程度仍不足。
- [ ] 没有主线模式与个人分支模式的明确视觉切换。

## 8. 技术问题

- [ ] 按周请求 `quality` 数据时，API 按质量系列本身的 `week` 过滤，但真实周次位于 `anchors` 中，可能返回空质量序列。
- [ ] 页面一次加载全部周次、全部模块和全部文件数据，没有按当前周及详情按需加载。
- [ ] 实验室主要逻辑集中在 `lab-timeline-page.tsx`，尚未按照技术方案拆分模块。
- [ ] 尚未实现分支读取、回合结算、文件物化、文件差异、路径比较、AI 复盘和用户数据删除 API。
- [ ] 当前测试主要覆盖鉴权、分支、材料和页面源码结构。
- [ ] 缺少状态引擎、文件 Patch、结局规则和 AI 输出的单元测试。
- [ ] 缺少主线、行动链、回合推进、文件差异和复盘的浏览器端到端测试。
- [ ] 尚未实现 API 延迟、结算失败、AI 调用和案例错误的完整观测能力。

## 9. PRD 与技术文档内部不一致

- [ ] PRD 一处写每个情景 12–16 张候选卡，验收标准和技术方案写 18–24 张。
- [ ] PRD 一处写四项 AI 能力画像，最终确认口径和技术 Schema 实际为五项。
- [ ] PRD 写 W0–W32，当前案例和页面主体采用 W1–W32。
- [ ] 案例名称在“汽车手机端控制应用上线项目”和“车主远程控车应用项目”之间不统一。
- [ ] 技术方案的“当前缺口”和“建议下一步”部分需要根据 Release 23 的实现状态更新。

## 10. 建议推进顺序

### 第一步：校准只读主线

1. 确认 W0/W1 口径和 6 个里程碑。
2. 为 15 个仪表逐一确定业务状态源、公式、阈值和展示定义。
3. 需求统计已配置化；继续移除燃尽图和 CCB 的临时推算逻辑。
4. 完成 10 个管理领域的真实状态摘要和详情结构。
5. 完成主线时点的未来信息可见性规则。

### 第二步：建立真实文件模型

1. 定义 32 份文件的结构化 Schema。
2. 先完成情景一涉及文件的真实内容和主线版本链。
3. 实现文件物化、JSON Patch、版本历史和字段差异。
4. 建立文件与仪表、领域、情景和知识库节点的双向关联。

### 第三步：打通情景一纵向闭环

1. [x] 实现四栏行动链编辑器。
2. [x] 实现三段式决策依据和草稿保存。
3. [x] 实现单回合结算 API 与确定性规则引擎。
4. [x] 更新分支状态、核心绩效仪表和文件版本；新材料继续按后续情景规则补充。
5. [x] 实现即时客观反馈和缺口类别。
6. [ ] 实现情景一结束、文件差异和主线路径比较。

### 第四步：复盘与扩展

1. 接入 AI 结构化复盘和降级机制。
2. 完成能力画像与知识库推荐。
3. 验证情景一体验后复用引擎接入情景二、情景三。
4. 完成多分支管理、数据删除、配额和可用性测试。

## 11. 建议的下一次讨论起点

下一次建议从以下顺序继续：

1. 继续补齐其他仪表的真实详情，优先“当前项目健康状态”和异常优先级。
2. 选择下一批真实文件，建议优先D13项目沟通记录、D20质量报告和D29进度预测。
3. 深化情景一的范围、质量、CCB 和风险派生视图，并补齐字段级文件 Patch。
4. 实现情景一结束后的主线/个人分支 Git 式路径和文件差异比较。
5. 真实迭代燃尽数据暂时后置，不阻塞情景一核心学习闭环。

如果继续逐项审查“15 个总览仪表的数据真实性”，每项仍需确认：

1. 状态源数据；
2. 计算公式；
3. 主线每周变化；
4. 分支行动影响；
5. 预警阈值；
6. 点击后的详细视图；
7. 关联项目文件和管理领域。

CCB、RACI、风险与需求详情、首批六份动态文件、行动链编辑器、云端草稿、回合提交和确定性状态引擎已经具备；下一步进入字段级文件 Patch 与主线/个人分支 Git 式比较。
